#include "main.h"
/**
*add -adds two integer and return result
*@a: int to be added to b
*@b: int to ber added to a
*Returnn: sum of a and b
*/
int add(int a,  int b)
{
int c;
c = a + b;
return (c);
}
